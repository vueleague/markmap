const a={title:"markmap-cli"},e=`<p><img src="https://img.shields.io/npm/v/markmap-cli.svg" alt="NPM"></p>
<p>Use markmap command-line in a local terminal.</p>
<h2 id="usage">Usage</h2>
<pre><code class="language-bash">$ npx markmap-cli markmap.md
</code></pre>
<p>You can also install it globally:</p>
<pre><code class="language-bash">$ npm install -g markmap-cli
$ markmap markmap.md
</code></pre>
<p>There is a watch mode so that you could edit the Markdown file and get updates on the fly:</p>
<pre><code class="language-bash">$ markmap -w markmap.md
</code></pre>
`;export{a as frontmatter,e as html};
